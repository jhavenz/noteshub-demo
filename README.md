# NotesHub

A simple notes app built with [Framework X](https://framework-x.org/)/[ReactPHP](https://reactphp.org/). 

Uses SSE to stream updates to multiple clients in real time. 

## Quick Start

```
docker compose up --build --watch
```

Open `http://localhost:8486` 
